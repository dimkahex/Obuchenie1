apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-catalog
  labels:
    app: postgres-catalog
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres-catalog
  template:
    metadata:
      labels:
        app: postgres-catalog
    spec:
      containers:
        - name: postgres
          image: postgres:15-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              value: "catalog"
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: db-secrets
                  key: postgres-catalog-password
            - name: POSTGRES_DB
              value: "catalog"
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
            - name: init
              mountPath: /docker-entrypoint-initdb.d
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: postgres-catalog-data
        - name: init
          configMap:
            name: postgres-catalog-init
      resources:
        requests:
          memory: "128Mi"
          cpu: "100m"
        limits:
          memory: "256Mi"
          cpu: "500m"
      livenessProbe:
        exec:
          command: ["pg_isready", "-U", "catalog"]
        initialDelaySeconds: 30
        periodSeconds: 10
      readinessProbe:
        exec:
          command: ["pg_isready", "-U", "catalog"]
        initialDelaySeconds: 5
        periodSeconds: 5
